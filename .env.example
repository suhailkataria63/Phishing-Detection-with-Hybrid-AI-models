# Backend API settings
APP_NAME=phish-detector-api

# Comma-separated CORS origins (local + deployed frontend)
CORS_ORIGINS=http://localhost:3000

# Optional single frontend URL (useful for Render env setup)
FRONTEND_ORIGIN=
